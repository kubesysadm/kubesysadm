apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubesysadm.fullname" . }}-test-connection"
  labels:
    {{- include "kubesysadm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubesysadm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
