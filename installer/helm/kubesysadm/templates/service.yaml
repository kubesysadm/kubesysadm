apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubesysadm.fullname" . }}
  labels:
    {{- include "kubesysadm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubesysadm.selectorLabels" . | nindent 4 }}
